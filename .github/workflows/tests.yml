name: Build and Test

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake g++

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug

      - name: Build test target arithmetic operators
        run: cmake --build build --target IIL_lexer_arithmetic_operators_test

      - name: Build test target comparison operators
        run: cmake --build build --target IIL_lexer_comparison_operators_test

      - name: Run test binary arithmetic operators test
        run: ./build/IIL_lexer_arithmetic_operators_test

      - name: Run test binary comparison operators test
        run: ./build/IIL_lexer_comparison_operators_test